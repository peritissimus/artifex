/**
 * Single source for the privacy policy's "last updated" date, shared by
 * `/privacy` and its Markdown twin so the two can never disagree.
 *
 * Bump this whenever the policy text changes.
 */
export const privacyUpdated = '21 August 2026';
