{
  "name": "artifex",
  "version": "1.9.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lighthouse": "pnpm build && pnpm preview --port 4173 & sleep 3 && lighthouse http://localhost:4173 --view --output=html --output-path=./lighthouse-report.html --preset=desktop && kill $!",
    "lighthouse:mobile": "pnpm build && pnpm preview --port 4173 & sleep 3 && lighthouse http://localhost:4173 --view --output=html --output-path=./lighthouse-mobile-report.html --preset=perf --throttling.cpuSlowdownMultiplier=4 --screenEmulation.mobile=true && kill $!"
  },
  "dependencies": {
    "gl-matrix": "^3.4.4",
    "glsl-noise": "^0.0.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "lighthouse": "^13.0.1",
    "prettier": "^3.6.2",
    "sass": "^1.93.2",
    "vite": "^6.3.5",
    "vite-plugin-glsl": "^1.5.3"
  }
}
